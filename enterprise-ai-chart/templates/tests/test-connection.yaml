apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enterprise-ai-chart.fullname" . }}-test-connection"
  labels:
    {{- include "enterprise-ai-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enterprise-ai-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
