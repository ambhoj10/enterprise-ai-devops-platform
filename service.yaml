apiVersion: v1
kind: Service

metadata:
  name: enterprise-ai-api-service

spec:
  selector:
    app: enterprise-ai-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: LoadBalancer
